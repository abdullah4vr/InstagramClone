import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  name: {
    alignSelf: 'center',
    fontWeight: 'bold',
    color: 'rgb(60,60,60)',
  },
});

export default styles;
